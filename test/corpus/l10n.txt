==================
Localize
==================

{% localize %}x{% endlocalize %}

---

(template
  (content)
  (localize_group
    (localize_block
      (localize_tag)
      (template
        (content)))
    (endlocalize_tag))
  (content))

==================
Localize On
==================

{% localize on %}x{% endlocalize %}

---

(template
  (content)
  (localize_group
    (localize_block
      (localize_tag)
      (template
        (content)))
    (endlocalize_tag))
  (content))

==================
Localize Off
==================

{% localize off %}x{% endlocalize %}

---

(template
  (content)
  (localize_group
    (localize_block
      (localize_tag)
      (template
        (content)))
    (endlocalize_tag))
  (content))

==================
Localize Hugging Its Delimiters
==================

{%localize%}x{%endlocalize%}

---

(template
  (content)
  (localize_group
    (localize_block
      (localize_tag)
      (template
        (content)))
    (endlocalize_tag))
  (content))

==================
Localize Nested
==================

{% localize on %}{% localize off %}y{% endlocalize %}{% endlocalize %}

---

(template
  (content)
  (localize_group
    (localize_block
      (localize_tag)
      (template
        (localize_group
          (localize_block
            (localize_tag)
            (template
              (content)))
          (endlocalize_tag))))
    (endlocalize_tag))
  (content))

==================
Localize With Another Argument
==================

{% localize maybe %}x{% endlocalize %}

---

(template
  (content)
  (localize_group
    (localize_block
      (localize_tag
        (unexpected_argument))
      (template
        (content)))
    (endlocalize_tag))
  (content))

==================
Localize With Two Arguments
==================

{% localize on off %}x{% endlocalize %}

---

(template
  (content)
  (localize_group
    (localize_block
      (localize_tag
        (unexpected_argument))
      (template
        (content)))
    (endlocalize_tag))
  (content))

==================
Localize Unclosed
==================

{% localize %}

---

(template
  (content)
  (localize_group
    (localize_block
      (localize_tag)
      (template
        (content)))
    (missing_endlocalize_tag)))

==================
Localize Filter
==================

{{ x|localize }}

---

(template
  (content)
  (template_variable
    (filtered_value
      (value
        (variable_attribute
          (identifier)))
      (filter_expression
        (filter))))
  (content))

==================
Unlocalize Filter
==================

{{ x|unlocalize }}

---

(template
  (content)
  (template_variable
    (filtered_value
      (value
        (variable_attribute
          (identifier)))
      (filter_expression
        (filter))))
  (content))

==================
Localize Filter With An Argument
:error
==================

{{ x|localize:1 }}

---


==================
Unlocalize Filter With An Argument
:error
==================

{{ x|unlocalize:1 }}

---

