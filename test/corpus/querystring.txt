==================
Querystring Empty
==================

{% querystring %}

---

(template
  (content)
  (querystring_tag)
  (content))

==================
Querystring With Key Value
==================

{% querystring page=2 %}

---

(template
  (content)
  (querystring_tag
    (identifier)
    (filtered_value
      (value
        (literal
          (number)))))
  (content))

==================
Querystring With Identifier And Key Value
==================

{% querystring remove_key page=2 %}

---

(template
  (content)
  (querystring_tag
    (filtered_value
      (value
        (variable_attribute
          (identifier))))
    (identifier)
    (filtered_value
      (value
        (literal
          (number)))))
  (content))

==================
Querystring With Expression Argument And As
==================

{% querystring request.GET page=2 as qs %}

---

(template
  (content)
  (querystring_tag
    (filtered_value
      (value
        (variable_attribute
          (identifier)
          (attribute))))
    (identifier)
    (filtered_value
      (value
        (literal
          (number))))
    variable: (identifier))
  (content))

==================
Querystring With Two Keys
==================

{% querystring page=2 size=3 %}

---

(template
  (content)
  (querystring_tag
    (identifier)
    (filtered_value
      (value
        (literal
          (number))))
    (identifier)
    (filtered_value
      (value
        (literal
          (number)))))
  (content))

==================
Querystring With A Key Twice
:error
==================

{% querystring page=2 page=3 %}

---


==================
Querystring With A Key Repeated Later
:error
==================

{% querystring a=1 b=2 a=3 %}

---
